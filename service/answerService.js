const { Answer } = require('../models');

module.exports = {
    // 유저가 가지고 있는 답변을 질문과 함께 연결하기
    getUserAnswers : async() => {
        try {

        } catch (error) {
            throw error;
        }
    }
}